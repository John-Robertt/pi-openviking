# 编码代理入口

## 项目

面向 [Pi Coding Agent](https://github.com/earendil-works/pi) 的
[OpenViking](https://github.com/volcengine/OpenViking) 长期记忆扩展。系统战略目标、外部责任边界、模块职责
与数据契约由 `docs/design.md` 定义。

## 背景

本项目正在按 `docs/design.md` 的目标架构重建。`v1/` 是重建前工作树（提交 `4308d8f`）的完整副本，含当时的
实现、验证、配置和文档，内部自洽且可独立运行。

它已冻结：不接受维护，不参与仓库的验证与发布，也不是任何当前事实的权威源。它的当前用途只有一项——v1 在
真实 Pi 与真实 OpenViking 上产生过的边界事实（API 行为、lifecycle 时序、失败模式）可作为待验证线索；这些
线索由真实运行结果确认或证伪后才能成为依据。

不要从快照批量恢复实现、配置或验证。确有可复用内容时，按当前责任边界重新引入单个文件。

`docs/roadmap.md`「OpenViking 出站端口」阶段的边界调查完成后，`v1/` 整体移除。

## 工作指引

开始任务时：

1. 阅读 `docs/roadmap.md`，确认当前阶段、它落地的模块责任与验收判定；
2. 阅读 `docs/design.md` 中与目标相关的模块，确认其战略目标、业务需求和职责边界；
3. 按 `docs/documentation.md` 的文档清单定位任务涉及的其他资料；文档的新建、移动和引用同样遵循该规范。

完成定位后，再进入相关代码、配置和测试。

工作边界：

- 实现工作落在 `docs/roadmap.md` 当前阶段声明的模块责任范围内，该范围之外的内容由后续阶段承担；
- 模块的架构定位、核心目标、业务需求与职责边界以 `docs/design.md` 为准，阶段不重新定义；
- 新增模块前，先确认它满足 `docs/design.md`「新模块准入」的条件；不满足时属于需要上报的决策；
- 外部边界（Pi、OpenViking）的机制、协议与失败语义由真实运行记录确定，不由代码或文档转述推断；
- 交付改动时，按当前阶段的验收判定提供对应证据。
